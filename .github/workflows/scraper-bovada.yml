name: Bovada Line Tracker

on:
  schedule:
    - cron: '20,50 * * * *'
  workflow_dispatch: 

permissions:
  contents: write  

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests playwright
          playwright install --with-deps chromium
          
      - name: Run Bovada Scraper
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python scraper-bovada.py
        
      - name: Commit and Push Bovada Log
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          if [ -f bovada_lines.json ]; then
            git add bovada_lines.json
            git commit -m "Update Bovada lines" || echo "No changes"
            git pull --rebase origin main
            git push
          fi